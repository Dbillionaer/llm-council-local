{
  "servers": [
    {
      "name": "calculator",
      "command": ["python3", "-m", "mcp_servers.calculator.server"],
      "description": "Basic math operations (add, subtract, multiply, divide)"
    },
    {
      "name": "websearch",
      "command": ["python3", "-m", "mcp_servers.websearch.server"],
      "description": "Web search for current information, recent events, and specific entities"
    }
  ]
}
